import React from 'react';
import styles from  './Button.module.scss';

const Button = () => {
    return (
        <div className={styles.buttonDiv}>
            <button className={styles.button}> TRY FREE LESSON </button>
        </div>

        )

};

export default Button;
